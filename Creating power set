#include<bits/stdc++.h>
using namespace std;
vector<int> a = {1, 2, 3, 4};
vector<vector<int>> ans;
void print(int ind, vector<int> &b)
{
    if(ind == a.size())
    {
        ans.push_back(b);
        return;
    }
    b.push_back(a[ind]);
    print(ind + 1, b);
    b.pop_back();
    print(ind + 1, b);
}
int main()
{
    vector<int> b;
    print(0, b);
    sort(ans.begin(), ans.end(), [](vector<int> v1, vector<int> v2) {return v1.size() < v2.size();});
    for(int i = 0; i < ans.size(); i++)
    {
        if(ans[i].size() == 0) {
            cout<<"EMPTY SET"<<endl;
            continue;
        }
        for(int j = 0; j < ans[i].size(); j++)
        {
            cout<<ans[i][j]<<' ';
        }
        cout<<endl;
    }
}
